apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "homedns.fullname" . }}-test-connection"
  labels:
    {{- include "homedns.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "homedns.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
